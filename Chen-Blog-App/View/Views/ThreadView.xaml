<UserControl x:Class="View.Views.ThreadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Pink">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox x:Name="TextThreadName" Grid.Column="0" Grid.Row="0" Text="Thread name"/>
        <TextBox x:Name="TextThreadDescription" Grid.Column="0" Grid.Row="1" Text="Thread Description"/>
        <Button x:Name="ButtonSubmit" Grid.Column="0" Grid.Row="2" Content="Submit" Click="ButtonSubmit_Click" />

        <ListBox x:Name="ListBoxThread" Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"/>
    </Grid>
</UserControl>
